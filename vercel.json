{
  "version": 2,
  "framework": null,
  "buildCommand": "rm -rf .flutter && git clone https://github.com/flutter/flutter.git --depth 1 -b stable .flutter && export PATH=\"$PATH:$(pwd)/.flutter/bin\" && flutter config --enable-web && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
