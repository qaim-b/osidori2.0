name: couples_finance
description: "Couples expense tracking app — manual-first, scalable."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10+1

  # State management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Routing
  go_router: ^14.8.1

  # Backend
  supabase_flutter: ^2.8.4

  # Charts
  fl_chart: ^0.70.2

  # Utils
  intl: ^0.20.2
  uuid: ^4.5.1
  csv: ^6.0.0
  excel: ^4.0.6
  path_provider: ^2.1.5
  share_plus: ^10.1.4
  image_picker: ^1.1.2
  collection: ^1.19.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator: ^2.6.3
  build_runner: ^2.4.14
  json_serializable: ^6.9.5

flutter:
  uses-material-design: true
  assets:
    - assets/images/
